Команды для запуска сервисов из командной строки
worker: celery -A config worker --loglevel=info --pool=solo/threads/eventlet + --concurrency=10
beat: celery -A config beat --loglevel=info
flower: celery -A config flower --post=5555

WSL Redis
redis-server / redis-server /etc/redis/redis.conf - запуск стандарт\конфиг-файл
redis-server --daemonize yes - фон
redis-server --port 6380 порт
redis-cli ping - пинг

sudo service redis-server start/stop/restart - действия
sudo systemctl enable redis-server - включение автозапуска при загрузке системы
sudo service redis-server status - статус

redis-cli - подключение
info
info keyspace
KEYS *
keys:
    *ключ*
    префикс:*
get <имя> - посмотреть значение

docker
docker-compose build
docker-compose up/down/restart --build (запуск, стоп, пересборка)
docker-compose up <имя>
docker-compose logs <имя>
compose config - чекнуть чо внутри


Сначала создать пользователя и бд
Потом запустить веб
Сделать миграции
Сделать команды

docker-compose up --build web
docker-compose exec web python manage.py migrate
docker-compose exec web python manage.py pop_cards
docker-compose exec web python manage.py pop_users
docker-compose exec web python manage.py pop_exch

docker-compose exec web bash - войти в контейнер чтобы написать команды
docker-compose exec web python manage.py createsuperuser - для админки

